{
  "name": "voidbackups-server",
  "version": "0.1.0",
  "private": true,
  "description": "VoidBackups server — WebAuthn auth, agent coordination, backup scheduling, notification engine",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../.env --watch src/index.ts",
    "start": "bun --env-file=../.env src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.13.3",
    "@simplewebauthn/server": "^13.3.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "~5.9.3"
  },
  "license": "MIT"
}
